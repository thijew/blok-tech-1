<!DOCTYPE html>
<html lang="en">

<%- include("../components/head"); %>

    <body>
        <%- include("../components/header"); %>

            <main>

                <h1>Reserveringen</h1>

                
                <ul class="adminContainer">
                    <% for (let i=0; i < 7; i++) { %>
                        <% const date=new Date()%>
                            <% date.setDate(date.getDate() + i) %>
                            <ul class="left">
                                <li><%= date.toLocaleDateString('nl-NL', { weekday: 'long' , month: 'long' , day: 'numeric' }) %></li>
                                <li>Reserveringen: <%= totalChickenReservations %></li>
                            </ul>
                            <ul class="right">
                                <li><%= weather.daily.weathercode[i] %></li>
                                <li><%- weather.daily.temperature_2m_min[0] + i %> °C tot <%-weather.daily.temperature_2m_max[0] + i %> °C</li>
                                <% if (weather.daily.rain_sum[i]> 0) { %>
                                <li><%- weather.daily.rain_sum[i] %> mm</li><% } %>
                            </ul>
                        <% } %>
                </ul>

            </main>

            <%- include("../components/footer"); %>

                <script src="scripts/weather-api.js"></script>
                <script src="scripts/weather-code.js"></script>

    </body>

</html>