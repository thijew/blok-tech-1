<!DOCTYPE html>
<html lang="en">

<%- include("../components/head"); %>

    <body>
        <%- include("../components/header"); %>

            <main>
                <h1>Reserveringen</h1>

                <div class="reserve-day">
                    <ul>
                        <% for (let i = 0; i < 7; i++) { %>
                            <% const date = new Date(); %>
                            <% date.setDate(date.getDate() + i); %>
                            <li><%= date.toLocaleDateString('nl-NL', { weekday: 'long', month: 'long', day: 'numeric' }) %></li>
                            <!-- <li>WEATHER API</li> -->
                            <!-- <li>Reserveringen: </li> -->
                          <% } %>


                    </ul>
                </div>
            </main>

            <%- include("../components/footer"); %>

                
    </body>

</html>


